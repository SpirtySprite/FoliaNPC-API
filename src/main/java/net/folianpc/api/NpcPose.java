package net.folianpc.api;

// Names match the server's own pose enum, so anything the server knows works even if unlisted here.
public enum NpcPose {
    STANDING,
    SLEEPING,
    SWIMMING,
    CROUCHING,
    SITTING,
    FALL_FLYING,
    DYING
}
