package net.folianpc.api;

// Short, canned gesture sequences played over a couple of seconds. Built entirely on the same
// look/animate packets used elsewhere, so they work on every server version this library supports.
public enum Emote {
    NOD,
    SHAKE_HEAD,
    WAVE,
    DANCE
}
